# SPA-fallback wordt bij een Workers-deploy geregeld via wrangler.jsonc
# (assets.not_found_handling = "single-page-application"), niet hier.
# Een /* -> /index.html 200 regel wordt door de Workers asset-router
# afgekeurd als oneindige lus (code 100324). Voeg hier alleen echte
# redirects toe indien nodig.