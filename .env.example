# Kopieer naar .env en vul in (Supabase → Project Settings → API).
# Alleen VITE_-prefixed vars zijn zichtbaar in de frontend.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key